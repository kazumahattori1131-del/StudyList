{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); FILE=$(echo \"$INPUT\" | jq -r '.tool_input.file_path // \"\"'); echo \"$FILE\" | grep -qE '/problems/.*\\.html$' && REL=\"${FILE#/home/user/StudyList/}\" && printf '{\"systemMessage\":\"GitHub Pages URL:\\nhttps://kazumahattori1131-del.github.io/StudyList/%s\"}' \"$REL\" || true",
            "statusMessage": "GitHub Pages URLを生成中..."
          }
        ]
      }
    ]
  }
}
